Sales MTD = TOTALMTD([Sales amount],'Date Table'[Date])
Sales QTD = TOTALQTD([Sales amount],'Date Table'[Date])
Sales YTD = TOTALYTD([Sales amount],'Date Table'[Date])
Sales Above Average = IF([Sales amount]>AVERAGEX(ALL(olist_products_dataset),[Sales amount]),"Above Avg","Below Avg")
Rolling 12 Months sales = CALCULATE([Sales amount],DATESINPERIOD('Date Table'[Date],MAX('Date Table'[Date]),-12,MONTH))
Running Total sales = CALCULATE([Sales amount],FILTER(ALL('Date Table'),'Date Table'[Date]<= MAX('Date Table'[Date])))
Sales Contribution Percentage = DIVIDE([Sales amount],CALCULATE([Sales amount],ALL(olist_products_dataset)))
Month Over Month Growth Percentage = DIVIDE([Sales amount]-[Previous month sales],[Previous month sales])
Sales amount = SUM(olist_order_items_dataset[price])
Total quantity = COUNTROWS(olist_order_items_dataset)
Average Sales Per Order = DIVIDE([Sales amount],DISTINCTCOUNT(olist_order_items_dataset[order_id]))
Total Orders = DISTINCTCOUNT(olist_order_items_dataset[order_id])
Total Customers = DISTINCTCOUNT(olist_customers_dataset[customer_id])
Year over year growth = DIVIDE([Sales amount]-[previous Year sales],[Previoius year sales])
Sales Last 90 Days = CALCULATE([Sales amount],DATESINPERIOD('Date Table'[Date],MAX('Date Table'[Date]),-90,DAY))
